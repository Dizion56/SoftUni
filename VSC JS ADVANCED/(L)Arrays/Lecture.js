// function evenElPos(arr){
//     let result = [];
//     for (let i = 0; i < arr.length; i+= 2) {
//         result.push(arr[i]);
//     }
//     console.log(result.join(' '));
// }

// evenElPos(['20', '30', '40','50', '60']);

function lastKSequence(n, k){
    let result = [1];
    for (let i = 0; i < n; i++) {
        
        
    }
}
lastKSequence(6, 3);
lastKSequence(8, 2);